$include "util_assertions.msl"

tests_start("REFERENCES")
function add(a,b){
  return a + b
}
let myref = ref add 
assert_eq(myref.invoke(10,20), 30, none)
assert_eq(invoke(myref,30,40), 70, none)
tests_success("REFERENCES")
