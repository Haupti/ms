#pragma once

// TODO implement the VM instruction type
// its similar to the IR instruction type
// but with labels removed and jump labels directly replaced with addresses
// same for variable and function names, that should be resolved to read/jump addresses as well
