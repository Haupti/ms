# TODO
* function like macros & -export macro to export those function like macros to the file that includes it (but only that one)
* interpreter accepts global flag definitions as command line arguments
